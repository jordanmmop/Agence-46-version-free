fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiofiles>=24.1.0
pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.50
requests>=2.32.0
# Vérification des certificats HTTPS depuis l'exe compilé. certifi fournit un
# magasin indépendant du système ; truststore lit celui de Windows/macOS —
# seul capable d'accepter l'autorité d'un antivirus qui inspecte le HTTPS.
# Sans eux : « CERTIFICATE_VERIFY_FAILED » sur chaque téléchargement.
certifi>=2024.2.2
truststore>=0.9.1 ; python_version >= "3.10"
python-dotenv>=1.0.0
# Vérification de la signature Ed25519 des licences Pro (python/licence/).
# Absente, aucune licence ne peut être vérifiée : l'application reste en
# version d'essai (échec fermé, voir licence/verification.py).
cryptography>=41.0.0
aiohttp>=3.10.0
websockets>=13.0
pydantic>=2.9.0
scipy>=1.14.0
scikit-learn>=1.5.0
httpx>=0.27.0
# Trading réel local (Windows uniquement) — bundlé dans l'exe au build.
# Ignoré sur Linux/Mac (CI, serveur) : le trading réel requiert Windows.
MetaTrader5>=5.0.45 ; platform_system == "Windows"
